load("basis-precalc/basis-pre-calc");

/*
for c : 1 thru 2 do (
  for v : c thru 3 do (
   fname : sconcat("~/max-out/basisSer", c, "x", v, "v", ".lisp"),
   writeBasisToFile(c, v, 4, makeSerendipBasis, fname)
  )
)$
*/

c : 2$
v : 1$
polyOrderMax : 4$
writeBasisToFile(c, v, polyOrderMax, makeSerendipBasis, sconcat("~/max-out/basisSer",c,"x",v,"v.lisp"))$
