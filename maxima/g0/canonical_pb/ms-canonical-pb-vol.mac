load("canonical_pb/canonical-vol")$
load(stringproc)$

pmax : 2$
for c : 1 thru 2 do (
   v : c,
   fname : sconcat("~/max-out/canonical_pb_vol_ser", c, "x", v, "v", ".c"),
   fh : openw(fname),
   funcName : sconcat("canonical_pb_vol", c, "x", v, "v", "ser"),
   calcCanonicalPBVolUpdater(fh, funcName, c, v, "ser", pmax),
   close(fh)
);